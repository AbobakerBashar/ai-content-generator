export const metadata = {
	title: "Authentication",
	description: "Sign in to your account or create a new one.",
};

const AuthLayoute = ({ children }) => {
	return <div>{children}</div>;
};

export default AuthLayoute;
